# loop-play-spring-ai-agent

Spring AI 기반 배달 상담 에이전트 학습용 스타터 코드입니다.

## 개요

루퍼스 부트캠프 "Spring AI 배달 상담 에이전트" 6주 과정의 Week 1 미션 스타터 코드입니다.
`ChatClient`, System Prompt, Structured Output, Streaming, Observability 개념을 실습합니다.

## 빠른 시작

```bash
./gradlew bootRun
```
---
# 1단계
- [x]  `RagConfig.java`의 `// TODO [1단계-A~D]` 4개를 채운다:
    - `TOP_K` 값 결정 (권장 4)
    - `SIMILARITY_THRESHOLD` 값 결정 (권장 0.5 — qwen3 임베딩 기준으로 도메인 질문 몇 개 돌려 보고 조정)
    - `TokenTextSplitter` Bean (청크 800 / min 350)
    - `QuestionAnswerAdvisor` Bean (`order(20)`)
- [x]  `KnowledgeLoader.java`의 `// TODO [1단계-E, F]` 2개를 채운다:
    - FaqDocument → `Document` 변환 + `tokenTextSplitter.apply()` + `vectorStore.add()`
    - `alreadyLoaded(faqId)` — `filterExpression("faqId == '...'")` 중복 방지
- [x] AssistantController.java와 SupportController.java의 // TODO [1단계-G, H]에서 Advisor 체인에 ragAdvisor를 추가한다 (순서: memoryAdvisor → ragAdvisor → performanceAdvisor)
- [x]  `BaedalPrompt.java`의 `// TODO [1단계-J]`에서 `[정책 인용 규칙]` 섹션을 직접 작성 (Fallback 문구 / 원문 수치 유지 / 상담 범위 밖 대응 / 복수 정책 우선순위)
- [x]  기동 로그에서 `RAG 시드 완료 — 신규 7건 / 스킵 0건`을 확인하고, 재기동 시 `신규 0건 / 스킵 7건`이 찍히는지 확인
    log : [KnowledgeLoader] RAG 시드 완료 — 신규 7건 / 스킵 0건 / 총 7건

- [x] 아래 시나리오 5종을 실행하고, 응답 본문과 DEBUG 로그의 Context: 블록 발췌와 vector_store 테이블의 row 수/카테고리 분포를 README에 붙여라.

| # | 시나리오 | 응답 본문                                                                                                                                                 | 사용 문서                           |
| --- |-----------|-------------------------------------------------------------------------------------------------------------------------------------------------------|---------------------------------|
| 1 | `"비 오는 날 배달이 늦으면 보상 받을 수 있나요?"` | "비 오는 날 배달이 늦어지면 보상 받을 수 있는 기준은 다음과 같습니다: [예상시간 별 내용] 하지만 비 오는 날이라는 이유만으로 보상은 어려우며, 기상 특보 발효 여부와 실제 지연 시간을 확인해야 합니다. 어떤 주문을 말씀하시는 건가요? 주문번호를 알려주시겠어요?" | "배달 지연 보상 기준", "기상 악화 시 배달 지연 안내" |
| 2 | `"결제 후 바로 취소하면 환불되나요?"` | "주문 상태가 CREATED 또는 ACCEPTED인 경우, 결제 후 즉시 취소해도 전액 환불이 가능합니다. 어떤 주문을 말씀하시는 건가요? 주문번호를 알려주시겠어요? 이렇게 알려주시면 정확한 정보를 제공해 드리겠습니다."                           | "환불 기본 정책"                      |
| 3 | `"쿠폰 중복 사용되나요?"` | "쿠폰 중복 사용은 다음과 같이 정해져 있습니다: [..] 어떤 쿠폰에 대해 궁금하신가요? 쿠폰 번호를 알려주시면 자세히 설명해 드리겠습니다."                                                                      | "쿠폰 사용"                         |
| 4 | `"사장님 전화번호 알려주세요"` | "사장님 전화번호 알려주세요"                                                                                                                                      | "RAG 없음"                        |
| 5 | `"Memory+RAG 협업: A: "2024-1234 배달 어디쯤?" → A: "아까 그 주문 환불 돼요?""` | "아까 그 주문은 배달이 완료된 상태라서 환불은 불가능합니다. 하지만 배달 지연 등 다른 문제 있으시면 알려주세요!" | 첫번째 질문엔 "RAG 없음", 두번째 질문엔 "환불 기본 정책","주문 취소 정책"   |

현재 주문 상태는 배달 중입니다. 따라서 환불은 배달 완료 후 24시간 이내에 가능합니다. 배달이 완료된 경우 사진 증빙을 첨부하여 문의해주세요.
만약 배달이 지연되고 있다면, 예상 도착 시간보다 60분 이상 지연될 경우 배달비 환불 또는 쿠폰 보상을 받으실 수 있습니다. 현재 시점에서는 환불이 불가능합니다.
환불을 원하시면 배달 완료 후 다시 문의해주시기 바랍니다.

## 설계결정문서
- [x]  **왜 청크 크기 800 / min 350인가?** 배달 정책 문서가 조항 단위로 이미 쪼개져 있다는 사실을 근거로 설명. “블로그 글 / 장문 PDF” 같은 다른 도메인이라면 어떻게 바꿀 것인가?  
    현재 사용되는 배달 정책 문서(md)의 경우, category(쿠폰,취소,환불,배달 등)별로 이미 쪼개져 있는 상태. 청크 크기를 800 으로 하는 경우, 위의 RAG 시드 로그를 확인했을 때 보이듯이 각 문서가 모두 하나의 Chunk 로 저장됩니다.  
    카테고리 별로 분류도 잘 되어있고, 문서를 더 잘게 나누어야 할 필요성이 없는 경우이기 때문에 문서 당 하나의 Chunk 가 되게끔 Chunk Size 를 셋팅했습니다.  
    추가적인 가설이지만 기본 "TokenTextSplitter" 가 아닌 "Seperator" 를 지정할 수 있게 한다면 "##" 등의 구분자로 구분이 가능할 것으로 보이나 이 경우, 나누어진 문서 내용에 대한 Metadata 가 프롬프트에 넘어가지 않기 때문에 더 정확도가 떨어질 것으로 예상됩니다.  
        ex. "QuestionAnswerAdvisor" 는 Document 의 Metadata 를 프롬프트에 추가하지 않음. 단순히 문서의 내용만을 추가하기 때문에 문서 하나당 하나의 Chunk 인 경우, 첫문장이 자연적으로 추가되기 때문에 문제가 없지만, 더 작은 Chunk 로 나눠지는 경우, 첫 문장이 빠지게 되면서 분류가 더 어려워질 것으로 예상됩니다.  
      
    장문 글을 저장한다면 Chunk 를 더 작은 단위로 나누고, 테스트로 어느 정도의 OverLab 을 적용할 것인지를 결정할 거 같습니다.  
    추가적으로 Chunk 마다 글의 분류값이나 제목, Tag 값을 metadata 로 추가하고 프롬프트에 추가될때 메타데이터도 같이 적히도록 변경할 것 같습니다.  
- [x]  **왜 Top-K=4인가?** 정책 문서가 7건인 상황에서 1 / 4 / 10 중 4를 선택한 근거를 설명  
    일단 저는 Top-K 를 3으로 변경했습니다.    
    그 이유는 위의 테스트에서 최대 사용되는 문서의 개수가 2개였던 점 + 문서별 분류가 확실하고, 각 분류단위로 최대 2개의 문서가 존재하는 점을 근거로 삼았습니다.    
    배달지연+환불 혹은 환불+취소 의 경우, 겹치는 부분이 생길 수 있을 것으로 판단되어 최대 3개로 잡았습니다.  
      
    top-k 값은 결국 최종 LLM 에게 전달한 문서의 개수를 정의합니다. 문서의 개수를 많이 주는게 꼭 좋은 응답을 보장하진 않고, 비용 증가는 확실하기 때문에 굳이 증가할 필요가 없다고 생각됩니다.
- [x]  **왜 `QuestionAnswerAdvisor.order(20)`인가?** `memoryAdvisor(10) → ragAdvisor(20) → performanceAdvisor(100)` 순서가 필요한 이유를 **시나리오 5를 예시로** 설명  
    현재 기준에서 Order 가 중요하다고 생각되지 않습니다.  
    현재 코드 기준으로 모델에게 넘어가는 프롬프트를 보면 RAGAdvisor 에게 이전 Memory Context 가 전달되지 않습니다. 이전 메모리 Context 는 최종 LLM 에게만 전달됩니다. 그렇다면 현재 기준으로 Order 는 큰 의미가 없다고 생각됩니다.  
    만약 프롬프트 생성 부분을 변경해서 이전 Context 가 RAGAdvisor 에게도 넘어가게 변경한다면 의미가 있을 것으로 보입니다.  
- [x]  **`similarityThreshold=0.5`의 근거.** 너무 낮으면 / 너무 높으면 각각 어떤 문제가 생기는가? (실험은 3단계 과제)
    너무 낮은 경우, 관련도가 떨어지는 문서까지 모두 검색되어, 정확도를 떨어뜨리고 토큰 사용량을 증대시킵니다.  
    너무 높은 경우, 사용자의 문의에 대해 유연성이 떨어집니다. 예를 들어 정확히 "배달 지연"이라는 문구가 없는 경우 문서가 검색되지 않을 수 있습니다.

# 2단계



# 3단계

## 관찰기록
"주문번호 2024-1234 배달 어디?" -> "아까 그 주문 환불 돼요?"

| # | 관찰 포인트 | memory(10) -> rag(20) | rag(5) -> memory(10) |
| --- | --- | --- | --- |
| 1 | "2턴 Context에 들어간 정책 카테고리" | "환불 기본 정책", "주문 취소 정책"  | "환불 기본 정책", "주문 취소 정책" |
| 2 | "Context 정책이 현재 주문(1234)와 관련있는가?" | 1234 주문의 상태를 알고 응답함.  | 1234 주문의 상태를 알고 응답함. |
| 3 | "LLM 응답의 정확도 (원문 수치 포함 여부)" | "현재 주문 상태는 배달 중입니다. 따라서 환불은 배달 완료 후 24시간 이내에 가능합니다. 배달이 완료된 경우 사진 증빙을 첨부하여 문의해주세요. 만약 배달이 지연되고 있다면, 예상 도착 시간보다 60분 이상 지연될 경우 배달비 환불 또는 쿠폰 보상을 받으실 수 있습니다. 현재 시점에서는 환불이 불가능합니다. 환불을 원하시면 배달 완료 후 다시 문의해주시기 바랍니다." | "대체적으로 비슷한 내용이지만, 중국어가 섞인 응답이 발생하기도 함. 이 부분은 모델성능의 문제로 판단됨." |

## 설계 결정 문서 (README에 작성)
- [x]  **왜 Memory Advisor가 RAG보다 먼저 실행되어야 하는가?** “아까 그 주문”을 예시로 한 설명을 쓰되, **프롬프트 조립 순서** 관점에서 말하라 (Memory가 먼저 이전 턴 메시지를 프롬프트에 넣으면 RAG는 그 “복원된 질문”을 임베딩한다)
    이번 Memory Advisor 를 후순위로 놓더라도 임베딩 모델에는 최종 UserMessage 만 포함됩니다. 현재 구조에서는 "그래야만 하는 경우가 아닌" 것으로 판단됩니다. 
    원인을 파악하기 위해 일부 코드를 살펴본 결과 공유합니다.  
   
   
 - `QuestionAnswerAdvisor` 를 RAGAdvisor 로 사용. before 부분에 보면 아래와 같은 코드가 있습니다.
```java
    // 1. Search for similar documents in the vector store.
    var searchRequestToUse = SearchRequest.from(this.searchRequest)
            .query(chatClientRequest.prompt().getUserMessage().getText()) // ChatClient 의 prompt 에서 임베딩에 사용할 Prompt 를 가져옴.
            .filterExpression(doGetFilterExpression(chatClientRequest.context()))
            .build();
    
    List<Document> documents = this.vectorStore.similaritySearch(searchRequestToUse);
```
- `Prompt 의 getUserMessage` 코드를 살펴봤습니다.  

```java
    public UserMessage getUserMessage() {
        for (int i = this.messages.size() - 1; i >= 0; i--) {
            Message message = this.messages.get(i);
            if (message instanceof UserMessage userMessage) {
                return userMessage;
            }
        }
        return new UserMessage("");
    }
```

- message 를 반환하는 부분에 보면 기존 messages 를 모두 append 해서 return 하는게 아닌 UserMessage 1건만 반환합니다.
- 실제 Debugging 시에는 이전 대화 이력이 모두 보이지만 해당 로직으로 인해 임베딩 모델에 입력되는 프롬프트는 최종 UserMessage 뿐입니다.
- 그럼 Ollama 에 요청을 보낼 때는 어떻게 이전 이력이 모두 포함되는지 `OllamaChatModel`을 조금 봤습니다.
```java
    Prompt buildRequestPrompt(Prompt prompt) {
		// Process runtime options
    ...
		return new Prompt(prompt.getInstructions(), requestOptions); // getIntructions 로 전체 프롬프트 내용으로 프롬프트를 build 함.
	}
```

- [x]  **반대 순서가 더 나은 상황이 존재하는가?** 예를 들어 “Memory에는 개인 정보가 들어있어 임베딩하면 안 되는 경우”를 고려해 보라 (Round 5 Guardrail 예고)
    제 테스트에서는 반대로 결과가 나왔기 때문에 반대의 경우를 고민해보겠습니다. (Memory 가 임베딩되어야 하는 경우)  
    테스트에서와 같은 단순 "이전 주문번호가 필요한 경우" 는 해당되지 않습니다. 주문번호는 관련 문서를 탐색하는데 영향을 끼치지 않습니다.  
    이전 대화가 "오늘 날씨 상태에 대해 대화했고" 최종 질문이 "그럼 지금은 환불 가능해?" 라는 방식으로 문의했다면 이전 대화 이력도 임베딩 하는게 정확도를 높일 수 있다고 생각됩니다.  
    실제 테스트 시에 RAG 결과 검색되는 문서는 `"# 기상 악화 시 배달 지연 안내"` 가 아닌 `"# 주문 취소 정책"` 였습니다.

| # | 문의 | 임베딩 검색 결과 | 최종 응답 |
| --- | --- | --- | --- |
| 1 | "갑자기 날씨가 악화된거 같은데 지금 배달 가능한거야?" | "# 기상 악화 시 배달 지연 안내" | "기상 악화로 인한 배달 지연 관련 안내 나옴"| 
| 2 | "그럼 지금 나 주문 취소 가능해?" | "# 주문 취소 정책" | "주문 취소가 가능한 상태는 주문 상태가 **CREATED** 또는 **ACCEPTED**일 때입니다. 현재 어떤 주문을 말씀하시는 건가요? 주문번호를 알려주시면 확인해 보겠습니다."| 

- [x]  실험 후 원래 순서(`order(20)`)로 복원하라

### Issue 1. "Structured Output"이 필요한 프롬프트와 "content"만으로 동작해도 되는 프롬프트가 충돌함.

**문제**
`주문` 관련 단순 문의에도 `구조화된 응답`이 반환됨.
```
summary: 현재 주문이 배달 중이 아니라면 취소가 가능합니다.
category: CANCEL
urgency: 정상
affectedParty: PLATFORM
nextAction: 주문 상태 확인 후 고객에게 안내
requiresHumanVerification: false
neededInfo: []
```

**원인**
프롬프트에 응답 포맷에 대한 설명이 있어, 구조화된 포맷으로 응답.

**해결**
구조화된 응답용 프롬프트와 단순 응답용 프롬프트를 별도로 분리

### Issue 2. [규칙] 과 충돌하여 Tool 호출이 되지 않음. (시나리오 2)

**문제**
`getOrderDetail` 이 호출되지 않아 원인 분석. 프롬프트 전문 + Tool 정보를 AI 로 분석한 결과,
`[규칙]` 의 "정보가 부족하면 되묻는다" 항목과 Tool 사용 지시가 충돌하는 것을 발견.

**원인**
주문번호가 명확히 제공되었음에도 LLM 이 "정보 확인이 필요하다" 고 판단하여 Tool 호출 대신 되묻는 응답을 반환.

**시도한 방법**
- `@Tool` description 에 "반드시 호출하라" 명시 → Tool 호출 됨 ✅
- 프롬프트 규칙 수정 → Tool 호출 됨 ✅

**최종 적용**
```
Tool Description 에 "반드시 호출하라" 로 둘 경우, 다른 Tool 과 혼동을 줄 수 있어, 시스템 프롬프트에 내용 추가 
Tool Description 을 변경하고, 다른 Tool 이 수행되는지 3회 정도 테스트 시에는 문제가 없었지만, '반드시' 를 명시하긴 위험부담이 있다고 생각됨.
- 주문번호가 명확히 제공된 경우, 되묻지 않고 바로 Tool을 호출한다.
```

### Issue 3. 취소 사유(reason) 누락 시 Tool 미호출. (시나리오 3)

**문제**
취소 사유를 찾을 수 없는 경우 Tool 이 호출되지 않음.

**해결**
`@ToolParam` description 에 기본값 힌트 추가 후 Tool 호출 정상 동작.

**시도한 방법**
- `required = false` 처리 → Tool 호출 안 됨
- description 에 기본값 명시 → Tool 호출 됨 ✅

**최종 적용**
```java
@ToolParam(description = "취소 이유. 고객이 언급한 취소사유를 그대로 사용한다. 명시적인 사유가 없으면 '고객 요청'으로 입력한다.")
```

## 설계 결정문서
- [x]  `OrderDetailView`는 내부 `Order` 클래스의 필드 중 **무엇을 의도적으로 뺐는가?** (예: `deliveryAddress`, `canceledReason`, `riderLocation` 중 일부) 그 이유는?
   > `deliveryAddress` 컬럼 값 삭제.  
   > DB → @Tool 반환값 → LLM 프롬프트에 포함 → LLM 처리 → 최종 응답  
   > deliveryAddress 는 LLM 에게 사용자의 개인정보가 그대로 넘어갈 수 있음.
- [x]  `@Tool`의 `description`을 **한국어로 썼는가, 영어로 썼는가?** 어떤 기준으로 결정했는가?
   > 한국어로 작성. 실제 성능을 테스트해보지는 않았음. 개인 프로젝트가 아닌 팀 단위 업무라고 가정할 경우, 한국어로 작성하는게 가독성을 높일 수 있다고 생각이 들어 한국어로 작성.
- [x]  `OrderTools`를 **하나의 클래스로 묶었다**. 분리해야 한다면 어떤 기준으로 나눌 것인가? (조회 vs 변경 / 주문 vs 결제) 현재 수준에서는 왜 하나로 충분한가?
   > 기본적으로는 도메인 단위로 분리한다. 단, **"항상 같이 쓰이는가"** 를 기준으로 묶는 것이 더 실용적이다.
   > - **너무 크게 묶기** → 불필요한 Tool 까지 LLM 에 전달되어 토큰 낭비 발생
   > - **너무 작게 분리** → 토큰 절약이 가능하지만, 어차피 같이 쓰이는 Tool 은 항상 함께 등록하게 되어 분리 의미가 퇴색됨
   > - ex) 배달 현황 조회 시 주문 상태가 필요하므로, 분리하더라도 두 Tool 을 모두 등록해야 함
   >
   > 따라서 같이 쓰이는 Tool 끼리 묶고, 독립적으로 쓰이는 Tool 은 분리한다.  
   > 현재는 주문 관련 Tool 이 항상 함께 쓰이므로 **하나의 클래스로 충분**하다.  

---
# 2단계

- [x]  `cancelOrder`의 `ALREADY_CANCELED` 분기(이미 `OrderStatus.CANCELED`인지 체크하는 부분)를 **통째로 제거**하라

| # | 시나리오                    | 사용 Tool | Outcome 결과                                                    |
|---|-------------------------| --- |---------------------------------------------------------------|
| 1 | "주문번호 2024-1236 취소해주세요" | cancelOrder  | `죄송합니다, 해당 주문은 이미 배달이 완료된 상태로 취소가 불가능합니다. 추가적인 도움이 필요하시면 알려주세요.` |
| 2 | "분기 있으면서 2번 취소 하는 경우"   | cancelOrder  | `주문번호 2024-1239의 배달 주문이 취소되었습니다. 추가로 도와드릴 사항이 있으신가요?` \| `이미 2024-1239번 주문은 취소된 상태입니다. 다른 주문을 취소하려면 다시 한번 알려주세요.` |
| 3 | "분기 없으면서 2번 취소 하는 경우"   | cancelOrder  | `주문번호 2024-1239의 주문이 취소되었습니다. 다시 한번 도와드릴 일이 있으시면 언제든지 말씀해주세요.` \| `주문번호 2024-1239의 주문이 취소되었습니다. 더 필요한 도움이 있으시면 알려주세요.` |

- [x]  같은 주문(`2024-1239`)을 연속 2회 취소 요청하라
   - [x]  코드는 어떻게 동작하는가? (예외 / 잘못된 값 / 두 번째 호출이 다른 분기로 흘러가는가)
   - [x]  LLM의 자연어 응답은 어떻게 바뀌는가?
   - [x]  로그에서 `canceledReason`은 어떻게 덮어쓰이는가?

    1. 별도 분기로 흘러 `ALREADY_CANCELED` 응답 발생    
    `ALREADY_CANCELED` 응답이 LLM 에 넘어가면서 "이미 배달이 완료된 상태로 취소가 불가능합니다." 라는 최종 응답 발생
    2.  `고객 요청` -> `메뉴 수량을 잘못 입력했습니다` 로 canceledReason 이 덮어쓰여짐. 다만, LLM은 최종적으로 `이미 해당 주문은 취소되어 있습니다. 다른 문의사항이 있으시면 알려주세요.` 로 응답.

- [ ]  **"고객에게 어떤 오해를 줄 수 있는가?"** 를 3가지 이상 작성  
    1. 고객의 경우, "주문이 취소되었습니다." 라는 응답을 받기 때문에 큰 영향은 없을 것으로 생각됨.  
    여러 번 취소요청 시마다 "취소되었습니다." 라는 응답 메세지를 받겠지만 "취소가 되었다"는 메시지기 때문에 큰 영향은 없을 것으로 보이나,  
    일부 고객의 경우, 확인을 위한 CS 요청이 들어올 수 있음.
- [x]  멱등성 분기를 복원한 뒤, **"이 로직이 없었다면 프로덕션에서 어떤 장애가 생겼겠는가"** 를 3가지 이상 작성  
  (예: 결제 이중 취소 / 포인트 이중 환급 / 사장님에게 취소 알림 두 번 / ...)
    1. canceledAt, canceledReason 값이 덮어써짐. 실제 취소 요청이 어떻게 이루어졌는지 추적이 불가능해짐.
    2. 실제 결제가 발생한 경우, 최악의 경우 결제가 이중 취소될 수 있음. 이 부분은 PG 사에서 별도 에러 처리를 해줄 수 있지만, 시스템 내부적인 포인트/쿠폰 환급 등은 이중으로 발생할 수 있음
    3. 실제 DB를 사용하는 경우, DB Write 액션이 불필요하게 발생하여 부하를 일으킬 수 있음.

---
# 3단계

| 버전   | Tool 호출 횟수 (5회 중)                          | 응답에 "역삼역 사거리" 포함 횟수 | 비고 |
|--------|--------------------------------------------|---------------------|----|
| A  | "getDeliveryStatus" 5회                     | 5                   |    |
| B  | "getDeliveryStatus" 3회 "getOrderDetail" 2회 | 3                   |  프롬프트에서 Tool 사용 권장  |
| B  | "getDeliveryStatus" 5회                     | 5                   | Tool Description만 사용   |
| C  | "getOrderDetail" 5회                     | 0                   |    |


## 설계 결정 문서 (README에 작성)
- [x]  버전 C에서 LLM이 어떤 행동을 했는가? (Hallucination / 잘못된 Tool / 질문 회피 / ...)  
    1. 잘못된 Tool을 사용함.
- [x]  **description 작성 시 반드시 포함해야 할 항목 4가지**를 본인 언어로 정리하라  
  (강의 자료의 4가지 — 무엇/언제/입력/실패 — 을 그대로 쓰지 말고, 본인이 실험으로 체감한 **중요도 순으로 재배열**해 설명)

| 시나리오    | Description               | Tool 호출 횟수 (3회 중) | 응답                                                                                                         | 비고 |
|---------|---------------------------|-------------------|------------------------------------------------------------------------------------------------------------|---|
| 언제      | "고객이 주문 취소를 요청할 때 사용한다."  | 0회                | "주문을 취소하시려면 정확한 주문 번호와 함께 취소 사유를 알려주시면 감사하겠습니다. 이번에 취소하실 주문의 번호가 2024-1234 맞나요?"                           | |
| 무엇 + 입력 | "주문을 취소한다. orderId 는 'YYYY-XXXX' 형식이다."  | 1회                | "존경한 고객님, 주문 번호 2024-1234의 배달을 취소하고자 합니다. 취소 사유를 말씀해주실 수 있으신가요? 또는 '고객 요청'으로 진행하셔도 됩니다." (취소된 경우는 기재하지 않음) | |
| 무엇 + 실패 | "주문을 취소한다. 주문이 없으면 null 을 반환한다."  | 0회                | "주문을 취소하시려면 확인이 필요합니다. 먼저, 해당 주문 번호로 주문 상세 정보를 확인하겠습니다. 주문 상세 정보를 알려주시겠어요? 또는 다른 방법으로 취소 사유를 말씀해주시면 감사하겠습니다." | |


    이전 테스트에서도 동일한 문제가 있었지만, 이번 테스트 중에 Tool 사용은 "반드시 사용하라" 와 같은 강제 사항을 프롬프트에 집어넣지 않고서는 Tool 사용이 이루어지지 않는 경우가 많았다.  
    이번 테스트 결과도 테스트 내용만 가지고는 "description" 작성 방법을 확인하기는 어려운 결과라고 생각됨.  
    Tool 사용을 강제하지 않는 경우, 대부분의 경우 Tool 이 사용되지 않음. 모델 자체의 성능에 따른 문제로 보임.
    Tool 사용을 강제하는 프롬프트를 추가하는 경우, 실험의 의미가 없다고 생각되어, 추가 테스트 진행하지 않았습니다.
    **테스트 결과가 유의미한 내용이 아니었기 때문에 이 부분은 현재 Qwen2.5 모델로는 답을 하기 어려운 문제라고 판단됩니다.**

- [x]  description이 "오래된 주석"처럼 실제 Tool 동작과 어긋나게 되는 상황을 프로덕션에서는 어떻게 막을 것인가? (힌트: 테스트, PR 리뷰, Contract Test, ...)
    1. 테스트 코드 작성  
    실제 LLM 을 태우지 않고, description 에 맞게 해당 메소드를 테스트 하는 코드 작성  
    description 이 변경되면서 메소드 내용이 변경되면 테스트에서 실패

    ```java
    @Test
    void cancelOrder_notFound_shouldReturnNotFound() {
      // LLM 호출 없이 Tool 메소드 직접 호출
      CancelOrderResult result = orderTools.cancelOrder("없는ID", "취소사유");
      assertThat(result.outcome()).isEqualTo(Outcome.NOT_FOUND);
    }
    ```
    2. Monitoring 추가. Tool 별로 호출된 이력을 모니터링하고, 자주 호출되지 않던 Tool 이 비정상적으로 많이 호출된다던가 하면 감지할 수 있도록 셋팅

---
# 공통 학습 기록
- [x]  **"내가 배운 것"**  
    Tool Calling 에 대해 알게 되면서 AI 를 활용할 뿐, 기존에 API 를 만드는 것과 비슷하다는 느낌을 많이 받았습니다.
    API 를 만들때처럼 멱등성을 지켜야하고, 문제가 되는 경우에 대해 예외처리를 코드에서 직접 처리하고 AI 는 실행만을 담당하는 것을 보면서
    막연히 "AI 를 어디까지 믿을 수 있나?"에 대한 해결책을 일부 본 것 같은 생각이 들어서, 개인적으로는 재밌었던 과제였습니다.
    트랜잭션이나 병렬 호출 등에 대한 제어도 코드 레벨에서 직접적으로 제어하고, AI 는 외부에서 호출하는 오케스트레이션 정도의 기능을 담당하게 되지 않을까 하는 생각도 들었습니다.
- [x]  **"의문점"** — 아직 해결되지 않은 궁금증
    이전 테스트 결과에서도 간단히 언급했지만, Tool Calling 이 생각보다 잘 되지 않는다는 느낌을 많이 받았습니다.
    프롬프트에 강제하지 않는 이상, AI 에서는 Tool 을 적절하게 호출하지 않는 것으로 보였는데 단순히 qwen2.5 모델만의 문제인건지 실제 다른 성능이 좋은 AI 를 사용하더라도 동일한지 궁금해졌습니다.
    성능이 좋은 모델일수록 Tool 을 잘 호출한다면, 실제 서비스에 적용하고자 하는 경우에는 다양한 모델에 직접 테스트를 해서 적절한 모델을 찾게 될까? 하는 의문도 들었습니다.
- [ ]  **"Round 3에 시도하고 싶은 것"** — Round 3 Chat Memory와 연결할 아이디어